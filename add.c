#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <ncurses.h>

int add(int a, int b) {
	return a + b;
}

int main(int argc, char **argv) {

	initscr();
	cbreak();

	refresh();

	if (argc < 3) return 1;

	int a = atoi(argv[1]);
	int b = atoi(argv[2]);

	int c = add(a, b);
	char c_cstr[16] = "\0";

	move(LINES/2, (COLS/2)-((strlen(c_cstr))/2));

	printw("%d\n", c);
	
	move(LINES-1, 0);

	refresh();
	
	endwin();

	return 0;
}























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































